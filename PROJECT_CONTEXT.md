# Amar Granth — Hydrogen Storefront: Project Context

This file is a handoff brief for continuing this project in Claude Code. Read this in full before making changes — it captures business context, decisions already made, and exactly what's done vs. not done in this codebase.

## Business context

**Amar Granth** (amargranth.com) is a children's illustrated mythology and heritage book publisher, run by ASM. ASM also runs a second, unrelated brand called **White Kailash** (Indian Khadi/handwoven textiles) on a separate Shopify store — do not confuse the two or mix their data.

- Target audience: millennial parents, ages 26–40, India-based, English + some Hindi
- Brand tone: emotional, story-driven, rooted in Indian heritage — not clinical or purely transactional
- Currency: INR. Timezone: IST.

## Migration history (why this project exists)

The old site (amargranth.com) was built on **Wix Classic Editor**. It's being migrated to a **Shopify + Hydrogen** stack for full code control. Migration work already completed by a prior Claude session (in claude.ai, not Claude Code):

1. **New Shopify store created**: "Amar Granth", domain `fd3ruf-f7.myshopify.com`, Basic plan, India/INR/IST, contact `contact@amargranth.com`. This is a **separate store** from White Kailash's Shopify store — don't mix them up.
2. **Catalog migrated**: 7 products, all currently in **DRAFT** status (intentionally not published — see "Go-live plan" below), with real inventory tracking, images, and SEO metadata copied over from Wix.
3. **6 collections created** to mirror the old site's "shop by collection" structure.
4. **66 blog posts migrated** into a Shopify blog (handle `blog`, title "Amar Granth Stories"), each recreated with the **exact same slug** as the old Wix post for SEO continuity.
5. **73 URL redirects created** in Shopify (66 blog + 7 product), mapping old Wix URLs to new Shopify URLs. These are already live in Shopify's redirect system and will activate automatically once the domain switches over — no further action needed on these.
6. **Domain (amargranth.com) is intentionally still pointed at the old Wix site.** Do not attempt to change DNS or go live until explicitly told the storefront is ready. This was a specific instruction from ASM to avoid any downtime or broken state on the live site.

**Not yet migrated** (out of scope for this codebase, flagging for awareness): static pages like About/Contact/FAQ from the old Wix site — these weren't accessible via API and will need manual content rebuild at some point.

## Shopify store reference data

**Store domain:** `fd3ruf-f7.myshopify.com`
**Blog:** handle `blog`, GID `gid://shopify/Blog/106927456511`

### Products (all currently DRAFT status)

| Title | Handle | Price | Stock |
|---|---|---|---|
| Shiva's Tears: A Children's Guide to the Rudraksha | `shivas-tears-a-childrens-guide-to-the-rudraksha` | ₹345 | 616 |
| The Chronicles of Lord Parashurama [Hardcover] | `the-chronicles-of-lord-parashurama-hardcover` | ₹495 (was ₹595) | 100 |
| A Children's Guide to the Rivers of Bharat [Paperback] | `a-childrens-guide-to-the-rivers-of-bharat-paperback` | ₹495 (was ₹595) | 50 |
| A Children's Guide to the 12 Shiva Jyotirlings [Hindi] [Paperback] | `a-childrens-guide-to-the-12-shiva-jyotirlings-hindi-paperback` | ₹295 (was ₹395) | 100 |
| 12 Jyotirlings + 51 Shaktipeeths Book Set [Hardcover] (the "Combo Set") | `12-jyotirlings-51-shaktipeeths-book-set-hardcover` | ₹845 (was ₹1,190) | 30 |
| A Children's Guide to the 51 Shaktipeeths [Hardcover] | `a-childrens-guide-to-the-51-shaktipeeths-hardcover` | ₹495 (was ₹595) | 200 |
| A Children's Guide to the 12 Shiva Jyotirlings [Best Seller] [Hard Cover] | `a-childrens-guide-to-the-12-shiva-jyotirlings-best-seller-hard-cover` | ₹495 (was ₹595) | 30 |

**The "(was ₹X)" figures above are now real, active `compareAtPrice` values in Shopify** (set via the Admin API on all 6 products that have one — Rudraksha never had a markdown). They weren't before: a real screenshot showed the PDP rendering a single accent-colored price with no strikethrough despite `DESIGN_SYSTEM.md`'s explicit discounted-price spec (struck-through original + final price in `--color-badge-sale`), and a direct Admin API check confirmed every one of the 7 variants had `compareAtPrice: null` — the "(was ₹X)" notation here had only ever been descriptive/historical (the old Wix price), never wired into Shopify's actual markdown field. `ProductPrice.tsx`/`ProductItem.tsx`'s discount-color logic (see the Fourth `DESIGN_SYSTEM.md` revision entry below) was already correct against real data; it just had no real markdown to react to until now. Confirmed with ASM before touching live pricing data.

### Collections

12 Jyotirlings · 51 Shaktipeeths · Rivers of Bharat · Combo Set · Rudraksha Guide · Parashurama

(The Shopify collection was previously titled "12 Jyotirlingas" — renamed via Admin API per `DESIGN_SYSTEM.md` Section 0.2's spelling rule, which flagged it as a known existing violation. Handle stays `12-jyotirlingas` unchanged, to avoid breaking any existing links — only the display title and its description copy changed.)

### Key business rule: ownership equivalence

**Combo Set buyers own both the 12 Jyotirlingas AND 51 Shaktipeeths content.** Anywhere the storefront does personalization, upsells, "you already own this" logic, or recommendations, a customer who owns the Combo Set must never be shown/recommended the standalone Jyotirlingas or Shaktipeeths titles as if they don't own them. They remain valid targets for Rivers of Bharat, Parashurama, and the Rudraksha Guide (titles they don't already own). This rule came directly from ASM and matters for any personalization work.

## Design system: "Storybook Studio"

This is an established, ASM-approved direction — do not deviate from the palette or typography without checking in. It's a soft, minimalist, kid-friendly aesthetic (chosen over two rejected alternatives: a warm "Modern Temple" direction and a dark "Editorial Heritage" direction).

**Colors** (already implemented as Tailwind v4 `@theme` tokens in `app/styles/tailwind.css`):
| Token | Hex | Use |
|---|---|---|
| `--color-base` | `#FEFCF7` | Page background (warm off-white, not pure white) |
| `--color-ink` | `#2B2B28` | Primary text |
| `--color-ink-soft` | `#5F5E5A` | Secondary/muted text |
| `--color-accent` | `#B45309` | Primary CTA color ("Terracotta Saffron") — the only color used for clickable/actionable things. **Full color history:** `#D9761F` Burnt Saffron (Sept 2026, to echo the old Wix theme) → darkened to `#AC5E19` (same month) after white-on-saffron measured only ~3.2:1, below WCAG AA's 4.5:1 → switched to Teal (`#1B8A7A`) per a re-uploaded `DESIGN_SYSTEM.md`'s Section 0 "hard constraints" (mandating teal, calling saffron a "drift" bug) → briefly nudged darker to `#197F71` for AA compliance, then reverted to the doc's exact literal `#1B8A7A` per ASM's explicit "follow the doc exactly" instruction, despite that literal value independently measuring ~4.23:1 (just under AA) → **current: `#B45309` Terracotta Saffron**, per yet another `DESIGN_SYSTEM.md` revision's Section 0/0.1 — the doc frames this exact swap as a deliberate test of its new "single-token architecture" rule (Section 0.1: every color defined once, in `tailwind.css`'s `@theme` block, changing the accent should touch only that one file). **Independently verified this time, not just trusted:** white text on `#B45309` computes to **5.02:1**, a real AA pass — unlike teal's 4.23:1, this one doesn't need an "accepted gap" caveat. If a future doc revision claims a contrast figure for a new color, verify it with real math (Python `colorsys`/luminance formula, or WebAIM's checker) before trusting the doc's own number — this doc revision explicitly self-corrected an earlier unverified "≈4.6:1" claim for exactly this reason. |
| `--color-accent-hover` | `#994708` | Accent hover state (~6.4:1, AA-compliant) |
| `--color-accent-active` | `#7E3A06` | Accent active/pressed state (~8.4:1, AA-compliant) |
| `--color-amber` | `#D9A441` | Decorative only (motif icons) — never used for CTAs or links, never carries text. Updated to this exact value alongside the teal switch (`DESIGN_SYSTEM.md` Section 0 lists it as one of eleven "literal and final" hex values). |
| `--color-border` | `#ECE7DB` | Card/input borders |
| `--color-tint-sand` / `-powder` / `-sage` / `-blush` | `#F3E6D8` / `#E4EEF6` / `#EAF1E4` / `#F6E6EC` | Pastel backgrounds for collection tiles/cards, cycled per item |

**Typography — superseded by `DESIGN_SYSTEM.md`, September 2026 (current, authoritative):**
- ASM has uploaded `DESIGN_SYSTEM.md` three times so far, each revision superseding the last — it's **the authoritative design/layout spec** for this project. Where it conflicts with anything written elsewhere (including earlier entries in this file's own history), the doc wins unless ASM says otherwise in chat.
- Typography per the doc is now a **3-part system** (as of the 3rd revision): `--font-display` (**Instrument Serif**, reserved for the single homepage hero headline ONLY, via the `.text-display` class — never used anywhere else), `--font-heading` (**Domine**, a sturdier serif for every other heading — h1/h2/h3/h4, plus the `.font-heading` utility class for non-heading elements that need the heading font, e.g. the logo wordmark), `--font-sans` (**Inter**, body/UI/buttons — button text always uses Inter, never a serif). This is the **5th** typography iteration this project has been through: `Fraunces`+`Nunito Sans` (original) → `Libre Caslon Text`+`Work Sans` ("premium," rejected) → briefly reverted to `Fraunces`+`Nunito Sans` ("playful") → a native-system-font `clamp()` spec (briefly implemented) → `Instrument Serif`+`Inter` for all headings → **current: 3-part Instrument Serif (hero only) + Domine (all other headings) + Inter**. All fonts loaded via one Google Fonts `@import` at the very top of `tailwind.css` (must precede `@import 'tailwindcss'`). Don't change this again without an explicit new request or doc update — and if a new doc revision arrives, check whether non-hero headings anywhere are still explicitly classed with `.text-display`/the old `.font-display` name, since that's the exact "too fancy" drift the doc itself warns about.
- Type scale is **discrete steps** (mobile value + a `@media (min-width: 1024px)` desktop override) — tokens: `--text-display` (34px→56px, hero only), `--text-h1` (28px→40px, weight 700), `--text-h2` (22px→28px, weight 700), `--text-h3` (18px→20px, weight 600, also used for h4), `--text-body-lg` (16px→18px), `--text-body` (15px→16px, body default), `--text-small` (13px→14px), `--text-micro` (12px, same both breakpoints). `h1`/`h2`/`h3`/`h4` get font-family/size/line-height/weight from global element rules in `tailwind.css` — any heading's `className` should stay free of explicit `text-*`/`font-*` size or family classes (higher specificity silently overrides the global rule; this bit the codebase once already — the earlier `.font-display` class was leaking Instrument Serif onto ~9 non-hero elements, including headings inside blog-article body HTML, before being caught and renamed to `.font-heading`/Domine).
- Body line-height 1.6.
- Logo wordmark (Header + Footer brand name): `font-heading text-[1.25rem] font-extrabold tracking-[-0.02em]`.
- Nav links/labels: `text-small font-semibold`.

**Layout principles established for this brand:**
- Flat navigation: logo + max ~4 links, no nested menus
- One accent color does all the "clickability" signaling (teal, see Colors above for history); amber is decorative-only (small motif icons like 🪔🪷🪶, never full-bleed patterns)
- Pastel-tinted tiles/cards instead of busy photography backgrounds — keeps things feeling calm and minimal
- Short, punchy copy over long paragraphs (this is a kids' brand — keep microcopy light and warm, not corporate)
- Rounded corners throughout (`--radius-card: 1rem`, `--radius-pill: 999px` for buttons/inputs)

## Current codebase state

Scaffolded with `npm create @shopify/hydrogen@latest` (TypeScript, Tailwind v4, React Router 7 / Hydrogen). Standard skeleton routes are all present and untouched except where noted below.

### Done
Every route and shared component is now built and styled in the Storybook Studio theme. **Deliberate deviations from `DESIGN_SYSTEM.md`'s literal text, made from real Codespaces screenshots (first real-browser verification this session got), each for a concrete reason rather than preference:**
- Product card actions ("View details" + "Add to cart"): doc says side-by-side, but that's now **stacked in a column** — side-by-side was genuinely squeezing/truncating the Add to cart button at this catalog's actual card widths (root cause: `AddToCartButton` renders a `<form>` as the real flex sibling, not the `<button>` inside it, so `flex-1` on the button was a no-op). `flex-col`'s default `align-items: stretch` fills both rows without any flex-1 tricks.
- Blog article hero image: doc says full container width (1280px), but it's now capped at **640px** (matching the body content column) — checked the real migrated image dimensions via the Storefront API and several are only 429–810px wide natively, so displaying them near 1280px was upscaling and visibly blurring them regardless of any code fix.
- Card-title (`h3`/`h4`) weight: doc specifies 600, dialed back to **500** — 600-weight Domine read too heavy/low-aesthetic on small card titles per direct feedback.
- Accent color is now **Terracotta Saffron `#B45309`** (was teal `#1B8A7A`) — see the Colors table above for the full history. This time the doc's own contrast claim was independently re-verified (5.02:1, a real pass), so there's no "accepted gap" caveat carried forward.
- Homepage "From the Blog" (doc 7.1 just says a generic teaser section): now shows **one row of 3 articles per blog category** (Jyotirlings/Shaktipeeths/Rivers of Bharat/Rishis & Sages/Temples/Rudraksha, skipping any category with zero matches) instead of N most-recent articles overall — one GraphQL query using 6 aliased `blog.articles(query:"tag:X")` sub-queries in `RECENT_ARTICLES_BY_CATEGORY_QUERY`.
- Search removed from the header entirely (button, aside, and the now-fully-unused `SearchFormPredictive`/`SearchResultsPredictive` components, deleted) per explicit "don't need it for now" — the standalone `/search` route still exists and still works (different, still-used `SearchForm`/`SearchResults` components), just not linked from the header anymore.
- Spelling: "Jyotirlingas" → **"Jyotirlings"** everywhere in on-site prose and the blog category label, per explicit request. The **real Shopify product tag itself is still `Jyotirlingas`** (confirmed via the Admin API, unchanged) — only display copy changed, so `getRelatedProducts`'s ownership-equivalence tag matching in `products.$handle.tsx` (which compares against the real tag value) is unaffected. If new copy ever needs to reference the tag value directly, remember the two now differ.

**Separately, a real bug (not a design deviation) was found and fixed once real screenshots were available:** `app/styles/reset.css` and `app/styles/app.css` are leftover Hydrogen-skeleton stylesheets loaded via their own unlayered `<link>` tags. Tailwind v4 puts its generated utilities in `@layer utilities`; unlayered CSS always beats layered CSS regardless of specificity, so bare rules like `a { color: #000 }`, `p { font-size: 1rem }`, `input { border: 1px solid #000 }`, and `aside p { margin: 0 0 0.25rem }` were silently overriding Tailwind's color/spacing utilities on links, paragraphs, and form inputs site-wide — invisible until the first real screenshot (a black "Shop the collection" button led to the discovery). Both files are now wrapped in `@layer base { ... }`, confirmed correct in the compiled CSS output. **If a future screenshot shows an unexpected default browser color/size, check these two files' layer wrapping before assuming it's a component-level bug.**

**Third `DESIGN_SYSTEM.md` revision (Sept 2026) — token architecture + several concrete fixes:**
- **Section 0.1, single-token color architecture (new structural rule, not just a style rule):** every color must be defined exactly once, in `tailwind.css`'s `@theme` block — no hex/`rgb()`/`rgba()`/named color anywhere else in `.tsx`/`.jsx`/`.css`. Ran the doc's exact grep scan (`grep -rEn '#[0-9A-Fa-f]{3,6}\b|rgb\(|rgba\(' app/ --include='*.tsx' --include='*.jsx' --include='*.css' | grep -v 'app/styles/tailwind.css'`) and fixed every real hit: the two leftover Hydrogen-skeleton files (`reset.css`, `app.css`) had several literal hex/rgba values in still-active CSS (`.overlay`'s dim scrim, the aside's box-shadow, `a`/`hr`/`input`/`code` element defaults, the account order-search form's borders, `--color-dark`/`--color-light` custom properties used by `MockShopNotice`) — these now reference real tokens (`var(--color-ink)`, `var(--color-border)`) or, for the two dead-code-only spots, `color-mix(in srgb, var(--color-ink) N%, transparent)` instead of raw `rgba(0,0,0,N)`. `--color-dark`/`--color-light` moved into `tailwind.css`'s `@theme` block (the one canonical file) rather than being redefined in `app.css`. Also deleted the dead, fully-unused `.header`/`.header-menu-*`/`.footer`/`.footer-menu`/`.overlay .light` rules from `app.css` (confirmed zero remaining references — Header.tsx/Footer.tsx are pure Tailwind) rather than "fixing" hardcoded colors inside code nobody renders. **This scan should be re-run before considering any future page/PR done** — it's cheap and catches exactly the kind of drift that caused the original brown-CTA bug the doc opens with.
- **Cart drawer footer excess bottom whitespace (real bug, doc explicitly flagged it):** `CartSummary`'s footer block used `mt-auto` inside a forced `h-full flex flex-col` split on `CartMain`/`.cart-details` — for a short (1–2 item) cart this pushed the footer all the way to the bottom of the drawer's full height, leaving a large dead gap above it. Fixed by removing the `h-full`/`flex-1`/`mt-auto`/inner `overflow-y-auto` split entirely — cart content (line items, then footer) now flows naturally inside `Aside`'s own `<main className="flex-1 overflow-y-auto">`, so a short cart's footer sits directly beneath the list, and a long cart (unlikely past ~3–4 items given this 7-product catalog) scrolls as a whole rather than the list scrolling independently of a pinned footer. `CartSummary`'s footer padding is now the doc's exact `--space-5` (24px, `p-6`) on all sides with `--space-3` (12px, `gap-3`) between its internal blocks (subtotal / discounts / gift card / checkout button), replacing the previous ad hoc `pt-5`/`mb-4`/`mb-3`/`mt-2` mix.
- **Cart drawer close/dismiss:** all three paths (explicit `×` button, overlay click, Escape key) were already implemented from an earlier pass — just updated the close button's `aria-label` to the doc's exact wording (`"Close cart"` / `"Close menu"`, derived from the `Aside`'s `type` prop rather than a generic `"Close"`).
- **Nav link weight:** doc calls for `--text-body` (16px) **weight 500** specifically, not 600 — `Header.tsx`'s `HeaderMenu` links were `font-semibold` (600); changed to `font-medium` (500) for both the desktop/mobile menu items and the mobile-only "Home" link (which had also been left at the old `text-small` size — now consistent `text-body` across the whole nav).
- **Homepage "Stories from Amar Granth" category rows:** each row (see the "From the Blog" deviation note above — this codebase's per-category-row structure already matched the doc's own restated version of that layout) now has a **"View all →" link at the row's end**, pointing to the real filtered blog listing URL `/blogs/blog?tag=<category-tag>` (matching the blog listing page's real `?tag=` URL param and blog handle `blog`) — this was missing before, only a single unfiltered "Read more stories →" link existed at the section level (kept, since the doc doesn't forbid it and it mirrors the Featured Products section's own top-level link).
- **Spelling cleanup (0.2, "always Jyotirlings, never Jyotirlingas"):** the Shopify **collection** titled "12 Jyotirlingas" — the doc's own explicitly flagged known violation — is now titled "12 Jyotirlings" (handle unchanged: `12-jyotirlingas`, to avoid breaking existing links; only display title + description changed). Also found and fixed 3 more real instances that had survived in live Shopify content from before this doc revision: the Combo Set **product**'s `descriptionHtml` ("12 Jyotirlingas" → "12 Jyotirlings", twice) and the Combo Set **collection**'s `descriptionHtml` (same fix). Re-grepped the codebase itself for "Jyotirlingas" — the only remaining hits are code comments and the `getComboUpsell`/`getRelatedProducts` tag-matching logic in `products.$handle.tsx`, which **correctly** still reference the real, unchanged Shopify tag value `Jyotirlingas` (see the tag-vs-spelling distinction noted earlier in this file) — nothing to change there.
- **Em dash guidance (0.2):** doc asks to simplify overused em dashes when touching existing copy, rather than as a blocking full-site sweep. Not done as a standalone pass this round — no existing copy needed touching for the fixes above beyond what's already listed.
- **Logo — real asset now in place.** ASM first pasted the logo inline in chat, which this environment cannot receive as a readable file (confirmed repeatedly — no file ever lands anywhere Claude Code can access, regardless of how it's pasted); a hand-drawn SVG recreation (`Logo.tsx`, built from reading the pasted image) was shipped as a temporary stand-in and flagged clearly as an approximation. ASM then pushed the real file directly into the repo via Codespaces as `app/assets/logo.svg` (a duplicate `mark.svg` and an accidental duplicate re-upload of the current `DESIGN_SYSTEM.md` also landed in the same commit — both removed as pure duplicates, zero new content). `Logo.tsx` now renders the real file directly (`<img src={logoUrl} />`, the same Vite-asset-import pattern used site-wide for image assets) — the hand-drawn recreation and its dedicated `--color-logo-cheek` token are gone. Wired into Header.tsx/Footer.tsx exactly as before (icon image + live "Amar Granth" text beside it, per Section 9 — the doc's `LOGO_BRIEF.md` file itself was never provided and still doesn't exist in the repo, but its stated intent — icon-only asset + live wordmark, not one flattened graphic — is satisfied either way). **Known follow-up, not blocking:** the real SVG is a vector wrapper around an embedded ~1980px-wide base64 PNG rather than true vector paths — 560KB on the wire for an icon that renders at ~32px in the header. Works correctly and is cached after first load, but worth asking ASM for a properly optimized export (a real vector, or at minimum a much smaller raster) if this ever becomes a performance concern.
- **Favicon fixed post-launch (real bug, not a design deviation):** the site was still shipping the generic Hydrogen-skeleton favicon (`app/assets/favicon.svg`, plain default line-art icon) — it was never swapped for the real logo when the sun mark landed, so every browser tab/bookmark showed a generic unbranded icon instead of the brand. Fixed by extracting the real transparent sun-mark PNG that's embedded inside `logo.svg` (the SVG wraps a raster image, not true vector paths — see the note above): it turned out the SVG contains two same-dimension embedded PNGs, a full-color version and a grayscale luminance mask, combined via the SVG's own filter effects to fake transparency. Composited them directly with Pillow (mask's luminance → alpha channel, color image's RGB → color channels) to get a genuinely transparent master PNG, then generated `favicon-32.png`, `favicon-192.png`, and `apple-touch-icon.png` from it. `root.tsx`'s `links()` now references these three (PNG, not SVG — browsers and search engines don't reliably support SVG favicons, and 560KB was too heavy to load as a favicon anyway) instead of the old generic `favicon.svg`, which has been deleted.

**Full-site sweep (same session, prompted by a real screenshot of the Refund Policy page showing zero page-container padding and an unstyled back-link):** it turned out several routes had **never actually been touched** in any earlier design-system pass — still the raw `npm create @shopify/hydrogen` skeleton markup (bare `<div>`/`<table>`/`<fieldset>` elements, no page-container padding, `<br />`-based spacing, meta titles still reading "Hydrogen | ..."). Rebuilt all of them in the Storybook Studio theme, reusing the same page-container/input/card/button conventions established elsewhere (see the "Done" bullets above) rather than inventing new patterns:
- **`app/routes/policies.$handle.tsx` + `policies._index.tsx`** — the policy detail page (Refund/Privacy/Shipping/Terms) and the policy index list. 640px prose column matching the blog article page, styled back-link, prose-wrapped policy body HTML.
- **`app/routes/account.tsx`** — the account section's shared layout (welcome heading, Orders/Profile/Addresses tab nav with an accent underline on the active tab, Sign out).
- **`app/routes/account.profile.tsx`, `account.addresses.tsx`, `account.orders._index.tsx`, `account.orders.$id.tsx`** — profile edit form, address CRUD forms (card-boxed per saved address), order search + order list (card-boxed order rows), and order detail (line items as a list matching the cart-line-item visual pattern, totals block, shipping address, status). All form inputs now use the site's established pill-shaped input style (`rounded-pill border border-border ... focus:ring-2 focus:ring-accent`) — the same treatment already used everywhere else on the site (discount code input, gift card input, email signup), rather than introducing a new input shape. Deleted the now-fully-unused `.order-search-*`/`.account-logout` rules from `app.css` (same reasoning as the earlier `.header`/`.footer` cleanup — confirmed zero remaining references first).
- **`app/routes/search.tsx` + `app/components/SearchResults.tsx`** — the standalone `/search` results page (title, input, and results now use the same pill input / card list conventions), and its Articles/Pages/Products result sections restyled to match the site's list/card patterns. Also fixed the meta title (was `"Hydrogen | Search"`).
- **`app/routes/pages.$handle.tsx`** — generic Shopify-Admin-authored page renderer (for any custom pages created in Admin outside the routes this codebase defines), same 640px prose treatment as policies. Meta title fixed (was `"Hydrogen | ..."`).
- **`app/root.tsx`'s `ErrorBoundary`** — this is what actually renders for *every* 404 and thrown error site-wide (the `app/routes/$.tsx` catch-all just throws a 404 `Response`, which bubbles up here). It was rendering as a completely bare, unstyled page with **no header or footer at all** — a real dead end for a mistyped URL or broken link. Now: a branded, on-theme message (different copy for 404 vs. other errors), a "Back to home" CTA, and — when the root loader itself succeeded (the common case; only a child route's loader threw) — the real site header/footer wrapped around it via `PageLayout`, so there's always a way to navigate out. Also stopped surfacing raw internal error text/stack traces to visitors (previously dumped via `<pre>{errorMessage}</pre>`) — shows a generic human message instead, per Section 10's "specific and human" error-copy rule and ordinary security hygiene (don't leak internal exception messages to end users).
- **New semantic token:** account/search forms needed a visually distinct "something went wrong" treatment (a failed profile update, a bad address) that the 11-color palette has no dedicated slot for — introduced `bg-tint-blush text-ink` (reusing the *existing* blush pastel token, not a new color) rather than reaching for an arbitrary Tailwind red, keeping Section 0.1's "every color defined once, only in `tailwind.css`" rule intact. This also fits Section 11's "color is never the only signal for state" rule better than a red/green convention would, since the actual error text is what carries the meaning.
- Re-ran the Section 0.1 hardcoded-color grep scan and the "Jyotirlingas" spelling grep across every file touched in this sweep — both come back clean.
- **Still not done:** the standalone `account_.login.tsx`/`account_.authorize.tsx`/`account_.logout.tsx`/`account.$.tsx`/`account._index.tsx`/`discount.$code.tsx` routes are pure redirects/loaders with no UI of their own (nothing to restyle there — Shopify's own Customer Account API login screen is a hosted, off-site flow this codebase doesn't control).
**Fourth `DESIGN_SYSTEM.md` revision (Sept 2026) — promotional color system + `site-config.ts`:**
- **Two new promotional colors**, `--color-badge-status` (`#0D7A5F`, Deep Teal-Green) and `--color-badge-sale` (`#C0294F`, Crimson) — independently verified via WCAG math (5.295:1 and 5.72:1 with white text, matching the doc's own claims exactly this time). Three purposes, three colors, never mixed: `--color-accent` (saffron) for actionable elements only, `--color-badge-status` for positive/social-proof tags (Bestseller/New), `--color-badge-sale` for time-bound urgency (Sale/event banner/discounted price). Fixed every place the codebase was using the wrong color for these: the homepage "Bestseller" badge was `bg-accent` → now `bg-badge-status`; the PDP event banner was `bg-tint-sand` (an earlier-revision spec, this doc explicitly calls it "too muted") → now solid `bg-badge-sale` fill with white text; `ProductPrice.tsx` and `ProductItem.tsx`'s price rendering didn't have a discounted-price color state at all (both `text-accent` unconditionally) → now `text-badge-sale` for the final price whenever a real `compareAtPrice`/`compareAtPriceRange` is present, with the struck-through original staying `text-ink-soft`.
- **Consolidated the homepage's duplicate product-card component:** `_index.tsx` had its own separate `FeaturedProductCard` (image/title/price/actions) that had silently drifted out of sync with the shared `ProductItem.tsx` used everywhere else (missing the compareAtPrice discount display entirely, using the wrong badge color). Deleted it; the homepage's "Featured products" section now renders the shared `ProductItem` with a new optional `badge` prop, so this class of drift can't recur.
- **New `app/lib/site-config.ts`** (Section 0.3's single source of truth for nav/social/content, same spirit as Section 0.1 for colors): centralizes `name`/`description`/`contactEmail`, header/footer nav (fallback only — see below), social URLs, the blog category taxonomy, bundle relationships, PDP trust-row copy, and a `promotions` schema field. **Several values deliberately differ from the doc's own illustrative example**, because the example conflicts with real, already-verified data — documented in the file itself: blog category tags are singular (`jyotirlinga`, `shaktipeeth`) to match the real tags on the 66 migrated articles (the doc's plural example tags don't exist on any real article and would silently return zero results); the Jyotirlingas collection href uses the real handle `12-jyotirlingas` (the doc's example `12-jyotirlings` 404s); `contactEmail` is `contact@amarshivmedia.com`, not the doc's `contact@amargranth.com` — ASM explicitly requested that exact change earlier in this project.
  - **Header/footer nav stays live-Shopify-menu-first.** `siteConfig.navigation.header`/`.footer` are used only as the *fallback* data (when Shopify's own Admin-managed menu query returns nothing) — previously each of `Header.tsx`/`Footer.tsx` hardcoded its own separate fallback object; now both build theirs from the one config, satisfying "not retyped in components" without removing ASM's ability to edit nav in Shopify Admin with no code deploy. Footer's `SHOP_POLICY`-typed fallback entries (Privacy/Refund/Shipping/Terms) stay hardcoded as-is, since they carry real Shopify resource GIDs — system identifiers, not the kind of human-editable content Section 0.3 is asking to centralize. Also deleted a fully dead, unused `activeLinkStyle` helper in `Header.tsx` that used hardcoded named colors (`'black'`/`'grey'`) — the automated Section 0.1 grep doesn't catch named colors, only hex/rgb, so this was found by reading, not the scan; worth remembering the scan has that gap.
  - **Bundle relationships (`siteConfig.bundles`) replaced the tag-based ownership/upsell logic** in `products.$handle.tsx`'s `getRelatedProducts`/`getComboUpsell` — previously driven by inferring relationships from `owns-jyotirlingas`/`owns-shaktipeeths`/`Jyotirlingas`/`Shaktipeeths` tags plus a price-math fallback to exclude the Hindi paperback edition; now driven directly by the real `comboHandle`/`includesHandles` product-handle pairs the doc specifies (verified against real Shopify data — same real pairing as before). This is simpler and more precise: the Hindi paperback edition isn't in `includesHandles` at all, so it never even reaches the savings-math check now, rather than reaching it and failing.
  - **Promotions stay metafield-driven, not `siteConfig.promotions`-driven.** The doc's example `promotions` array is empty/commented ("populate with real dates when a promotion is actually scheduled") — but this codebase already has a *working, populated* mechanism from the previous revision: real Shopify metafields (`custom.promo_label`/`promo_discount`/`promo_end_date` on each `Product`, editable in Admin with zero code deploy) driving the PDP's date-gated event banner, currently showing the real "Ganpati Festive Sale" data through 30 Sept 2026. A metafield is an even more direct fit for Section 0.3's own stated goal (content editable "without a full code review") than a TypeScript array requiring a git commit to change — so metafields stay the real mechanism; `siteConfig.promotions` exists only for schema completeness per the doc and is intentionally left empty, documented as such in the file.
  - Ran both the Section 0.1 (hardcoded colors) and the new Section 0.3 (hardcoded social URLs, `grep -rn 'facebook.com\|instagram.com' app/ | grep -v site-config.ts`) grep scans — both come back clean. The Section 0.3 scan's only non-`site-config.ts` hits are the Meta Pixel's own tracking-domain URL and CSP allowlist entries (`MetaPixel.tsx`, `entry.server.tsx`) — different-purpose URLs that happen to contain the substring "facebook.com," not a duplicate of the social profile link the rule is actually targeting.
- `app/routes/_index.tsx` — homepage, current section order per ASM's explicit follow-up instructions (superseding the earlier "merge both" 6-section version): **Hero** (real Combo Set product image/price/link, `.text-display` italic headline), **Featured products** (first 8 products, real `tags`-based "Bestseller" badge, quick-add card actions), **From the blog** (6 most recent articles = 2 rows on desktop, sorted recent-first), **Brand story** (the full real mission copy — same paragraphs as `/about`'s "Why we do this"/"What we make" sections, shown directly rather than teased behind a link), **Email signup**. The **"Shop by collection" section was explicitly removed** by ASM — its query (`FEATURED_COLLECTIONS_QUERY`) was deleted too, don't reintroduce it without being asked. `ArticleItem` was extracted from `blogs.$blogHandle._index.tsx` into a shared `app/components/ArticleItem.tsx`. Brand name displays properly cased ("Amar Granth") in Header/Footer.
  - **Reviews section is intentionally NOT built.** `DESIGN_SYSTEM.md` calls for a reviews section with real customer names/locations and explicitly says never to fabricate content; no real reviews exist yet. Add it once ASM supplies actual customer quotes.
  - **Mobile optimization pass (Sept 2026, from real Codespaces screenshots)** fixed 4 concrete issues ASM flagged, all in `_index.tsx`: (1) the hero product image was cropping top/bottom on mobile — its `aspect-[4/3]` container (both the populated `comboSet.featuredImage` branch and the empty-state fallback) forced a landscape crop on inherently square product photography; changed to `aspect-square` in both branches to match how product images are framed everywhere else on the site. (2) Excess whitespace between Hero and Featured Products — the Hero `<section>` had its own `py-16 lg:py-24` (top + bottom padding) while `FeaturedProducts` also applied its own top-margin `SECTION_GAP`, double-stacking the gap; changed Hero to `pt-16 lg:pt-24` (top only), consistent with every other section-to-section transition on the page, which relies solely on the following section's top margin. (3) The Featured Products grid was `products.slice(0, 8)` against a real 7-product catalog — a no-op slice that still left an odd/dangling card count on mobile's 2-column grid; changed to render all 7 real products via `products.map(...)` plus a new 8th tile, `SeeAllCard` (a `card`-styled tile linking to `/collections/all`, matching `ProductItem`'s visual footprint via CSS Grid's default `align-items: stretch` so it aligns with its row-mate with no extra CSS), giving a clean even count at every breakpoint. Also tightened the grid gap `gap-5`→`gap-4` (24px→16px, still within `DESIGN_SYSTEM.md` Section 2's allowed 16–24px range) to reclaim a little card width. (4) Product-card title truncation on narrow mobile cards was investigated (compiled CSS confirmed `line-clamp-2` itself was working correctly, ruling out a CSS bug) and treated as a real content-width tradeoff addressed by the gap reduction above, not a separate code fix — card padding stays at the doc's 16px mobile floor, which is non-negotiable per Section 2. **Issue 5 ("logo not replaced") required no code change** — `Logo.tsx` was re-verified to already correctly import and render the real `app/assets/logo.svg` asset (see the Logo entry above); the most likely explanation is a stale Codespaces build/cache, not a bug in this codebase.
- **Card anatomy** (`DESIGN_SYSTEM.md` Section 5.2, applies to every product/blog/collection card site-wide): images are full-bleed/edge-to-edge with zero padding (padding only wraps the text content below), titles 2-line-clamp with a reserved min-height so cards in a row stay aligned, and **product cards** (not blog/collection tiles) get a "View details" + "Add to cart" footer — quick-add works directly since the catalog is single-variant throughout, shows an "Adding…"/"Added ✓" loading state, and opens the cart drawer. Shared logic lives in `app/components/ProductCardActions.tsx`; `AddToCartButton` now accepts a function-as-children render prop for the dynamic label. Every product-card GraphQL fragment (`ProductItem`, `CollectionItem`, `HomepageProductItem`, `RelatedProductItem`) now fetches `variants(first: 1)` to supply the quick-add variant id.
- **Product detail page** (`products.$handle.tsx`) rebuilt per the doc's Section 7.3: breadcrumb, 58/42 two-column layout (image left, info right; stacks on mobile), a real multi-image gallery with clickable thumbnails (products already have 2–3 real photos each from the Wix migration, queried via `product.images`), full-width 52px add-to-cart button, a real shipping/returns trust row, an expandable "Shipping & returns" accordion, and a sticky mobile add-to-cart bar that appears once the primary CTA scrolls out of view (via `IntersectionObserver`). A "You might also like" related-products rail enforces the Combo Set ownership-equivalence rule using the real product tags already in Shopify (`owns-jyotirlingas` / `owns-shaktipeeths` on the Combo Set, `Jyotirlingas` / `Shaktipeeths` on the standalone titles) — not a hardcoded exclusion list, so it stays correct if the catalog changes. **Not implemented:** the doc's customer-specific "you already own this" badge — that needs Customer Account API / order-history integration this codebase doesn't have yet.
  - **Second audit pass (Sept 2026 doc revision) closed 5 gaps a WIP review found missing:**
    - **Quantity selector** — stepper (pill border, 44px targets, matches the cart drawer's pattern) directly above Add to Cart, defaults to 1, lifted as component state and threaded into both Add to Cart and Buy Now.
    - **Buy Now button** — full width, 52px, accent-outline secondary style, below Add to Cart. Skips the cart drawer entirely: `AddToCartButton` gained an optional `redirectTo` prop that renders a hidden `redirectTo` field; `cart.tsx`'s action treats the literal value `'checkout'` as a sentinel and, after the real `LinesAdd` mutation completes, 303-redirects to that mutation's own `cartResult.checkoutUrl` (not a hardcoded URL) — so it's a real Shopify checkout for a real cart, just without a stop in the drawer.
    - **Event/offer banner, metafield-driven, replacing the baked-in Wix promo copy:** created 4 new Shopify metafield definitions on `Product` (`custom.promo_label`, `custom.promo_discount`, `custom.promo_end_date` — type `date` — and `custom.whats_inside_bullets`, a `list.single_line_text_field`), all with Storefront API read access. Populated them from the **real, already-live "Ganpati Festive Sale" copy** that was sitting in `descriptionHtml` (label "Ganpati Festive Sale", each product's real discount — 17%/25%/30% depending on title — and the real end date 30 Sept 2026), then **removed that promotional sentence from the live `descriptionHtml`** via `productUpdate` on the 6 affected products (Rudraksha never had the promo). The PDP's `loadCriticalData` computes `promoActive` server-side (`promoEndDate >= now`, using the request's own clock, not a client `Date()` — avoids hydration mismatch) and the banner only renders when true; it will silently stop appearing after 30 Sept 2026 with no further code changes needed. To run a future promotion, set these 4 metafields on the relevant products instead of hand-editing descriptions.
    - **"What's inside" rebuilt as bullets-only:** now renders `custom.whats_inside_bullets` (a real JSON array, parsed client-side) instead of the raw `descriptionHtml`. The bullets themselves are the actual feature lists that were already in each product's live description (e.g. Rudraksha's "What Kids Will Learn" items, the Combo Set's "What's Inside" items) — curated down to 3–5, not invented. `descriptionHtml` is no longer rendered anywhere on the PDP.
    - **Combo/bundle upsell callout**, tag- and price-driven (`getComboUpsell` in `products.$handle.tsx`): shown only on a standalone product tagged `Jyotirlingas` or `Shaktipeeths` (not the Combo Set itself), and only when a real counterpart product exists whose price, combined with this product's price, genuinely exceeds the Combo Set's ₹845 — so it correctly appears on the Jyotirlingas Best Seller Hardcover and the standalone Shaktipeeths Hardcover pages (₹495+₹495−₹845 = real ₹145 savings) but **not** on the Hindi paperback Jyotirlings edition (₹295+₹495 = ₹790, which is *less* than the combo price — no genuine savings, so no fabricated claim). No hardcoded product pairing; stays correct if prices or the catalog change.
  - Sticky mobile add-to-cart bar (see above) still wraps the same CTA block, now including the quantity selector — behavior unchanged, still not yet confirmed in a real mobile browser (see "Real browser/device verification" below).
- **Collection/listing pages** (`collections.$handle.tsx`, `collections.all.tsx`, `collections._index.tsx`) rebuilt per Section 7.2: breadcrumb, a price/newest sort `<select>` (URL-param-backed, server-rendered via Storefront API `sortKey`/`reverse`), the doc's 2/3/4-column responsive grid, and compare-at-price strikethrough on product cards (new `compareAtPriceRange` field added to every product-card GraphQL fragment: `ProductItem`, `CollectionItem`, `HomepageProductItem`). Pagination/"load more" controls were removed on these three routes — the doc explicitly says not to paginate a catalog this size (7 products total).
- **Cart** (`CartMain.tsx`, `CartLineItem.tsx`, `CartSummary.tsx`, `cart.tsx`) updated per Section 7.4: 44px quantity-stepper tap targets, 64px line-item thumbnails, subtotal at `text-h3` weight, a full-width 52px "Checkout" button (was "Continue to checkout"). The doc's free-shipping progress indicator ("add ₹X more for free shipping") was intentionally **not** added — shipping is already free on every order with no minimum threshold, so a progress bar would need a fabricated number to count toward.
- **Blog listing** (`blogs.$blogHandle._index.tsx`) rebuilt per Section 7.5: header block, a category filter (Jyotirlingas / Shaktipeeths / Rivers of Bharat / Rishis & Sages / Temples / Rudraksha) driven by the **real tags already on the 66 migrated articles** (`jyotirlinga`, `shaktipeeth`, `rivers`, `rishis`, `temples`, `rudraksha` — verified via the Admin API, not invented), URL-param-backed (`?tag=`) so filtered views are shareable/bookmarkable, real per-article excerpts (Storefront API's `content(truncateAt: 140)`, not fabricated), 16:9 cards, the doc's 3/2/1 responsive grid, and 12-per-page "Load more". The taxonomy and its tag/product mappings live in the new `app/lib/blogCategories.ts`.
- **Blog article page** (`blogs.$blogHandle.$articleHandle.tsx`) rebuilt per Section 7.6: body copy constrained to the doc's 640px max-width, and a real product cross-link callout card ("Shop this book") for the four categories that map cleanly to one title (Jyotirlingas → the bestseller hardcover, Shaktipeeths, Rivers of Bharat, Rudraksha — Rishis & Sages and Temples intentionally have no cross-link since no single product matches), plus a related-posts row pulled from the same category tag.
- `app/components/Header.tsx` / `Footer.tsx` — flat nav header, now **sticky with a shrink-on-scroll effect** (padding tightens past an 80px scroll threshold) and a 1024px (`lg:`) breakpoint for the desktop nav/mobile-menu split (previously 768px/`md:`); cart count badge restyled as an accent-filled pill. Footer includes a brand blurb, a Collections link (added to the live Shopify footer menu via Admin API alongside About/Contact/FAQ/policy links), and copyright, with the doc's `space-8` (64px) vertical padding.
- `app/routes/products.$handle.tsx` + `ProductImage`/`ProductPrice`/`ProductForm`/`AddToCartButton` — pastel image tile, price + real `seo.description` hook, pill variant chips, quantity selector, Add to Cart + Buy Now, metafield-driven event banner, bullets-only "What's inside", combo upsell callout (see the second-audit-pass entry above for detail).
- `app/routes/collections._index.tsx`, `collections.$handle.tsx`, `collections.all.tsx` + `ProductItem` — pastel tile grids matching the homepage.
- `app/routes/blogs._index.tsx`, `blogs.$blogHandle._index.tsx`, `blogs.$blogHandle.$articleHandle.tsx` — article grid + single-post styling over the 66 already-migrated posts.
- Cart drawer/aside (`Aside.tsx`, `CartMain.tsx`, `CartLineItem.tsx`, `CartSummary.tsx`) and the standalone `/cart` page.
- **Static pages** `about.tsx`, `contact.tsx`, `faq.tsx` — real, ASM-confirmed content (not placeholders): free shipping across India (24–48hr dispatch), damaged-book returns via email within 24hrs, all major payment methods, age 3+/read-along positioning. `contact@amarshivmedia.com` is the only contact channel listed in-app (no phone/address exist yet) — this was changed from `contact@amargranth.com` per ASM's explicit request. Note: Shopify's own `shop.email`/`contactEmail` field has no Admin GraphQL mutation (confirmed — no `shopUpdate` mutation exists at all in the schema), so that field must still be changed manually in Admin → Settings → General if it hasn't been already.
- **All 7 products are ACTIVE** in Shopify (flipped from DRAFT on ASM's confirmation — see business context above, this was previously intentionally held back).
- **Typography**: swapped from the original Fraunces + Nunito Sans to **Libre Caslon Text + Work Sans** per ASM's explicit request (Fraunces read as "too curvy" for the premium-but-kid-friendly direction they want). Tokens live in `app/styles/tailwind.css` (`--font-display`, `--font-sans`) — nothing else should need touching if this changes again.
- **Meta Pixel** (`app/components/MetaPixel.tsx`, wired into `root.tsx`): storefront-side tracking (PageView, ViewContent, Search, AddToCart, InitiateCheckout) using pixel ID `620588632962566`. **Confirmed firing on production** (Sept 2026, post-launch) via Meta Pixel Helper — required setting `PUBLIC_META_PIXEL_ID` as an Oxygen environment variable in Shopify Admin → Hydrogen app → Environments and variables (Production scope) and redeploying; it isn't picked up from the local `.env` file automatically the way local `npm run dev` reads it. First check showed "no pixels found," which was simply the env var not yet being set on that deployment — not a code bug (`MetaPixelBaseScript` silently no-ops when `pixelId` is undefined). CSP in `entry.server.tsx` allows `connect.facebook.net`/`www.facebook.com` alongside Hydrogen's defaults. **Purchase/checkout-completion tracking is still NOT covered by this** — that requires ASM connecting Shopify's own Facebook & Instagram sales channel in Admin with their Meta Business account (self-serve, needs their login, can't be done via API).
- **Google Analytics 4** (`app/components/GoogleAnalytics.tsx`, wired into `root.tsx`): same pattern as the Meta Pixel — page_view, view_item, search, add_to_cart, begin_checkout — using measurement ID `G-EV1STZNVGV`, stored as `PUBLIC_GA4_MEASUREMENT_ID` in `.env` locally and as an Oxygen environment variable in production (Shopify Admin → Hydrogen app → Environments and variables). CSP extended for `googletagmanager.com`/`google-analytics.com`. Same purchase-tracking caveat as Meta: needs ASM connecting Shopify's "Google & YouTube" sales channel in Admin.
- **Google Merchant Center ID: `778061429`.** Not referenced anywhere in this codebase and shouldn't be — unlike the Meta Pixel/GA4 IDs, a Merchant Center ID isn't embedded in storefront code; Shopify's "Google & YouTube" sales channel handles the product feed sync to Merchant Center server-side once connected in Admin. Recorded here purely for reference in case ASM or a future session needs it (e.g. to check sync status, debug a rejected product, or set up Shopping ads).
- **Payments**: ASM has an existing, live Razorpay merchant account (already used on the Wix site) and has started adding it in Shopify Admin → Settings → Payments (saw "Cards Onsite by Razorpay" as one listing — likely one of several per-payment-method Razorpay entries in Shopify's India marketplace; UPI/netbanking may be separate listings to add too). No Hydrogen code involved — this is entirely Shopify Admin configuration using the same Razorpay Key ID/Secret already in use on Wix. Not yet confirmed working end-to-end (would need a test order to verify via the Admin API's order/transaction gateway field, or ASM confirming the "Active" status in Admin).
- Mobile responsiveness pass — most pages were already mobile-first; fixed touch-target sizing (cart stepper, close buttons, header icons) and text-wrap issues (FAQ accordion).
- **Pre-launch SEO/AEO pass** (Sept 2026, prompted by ASM asking to check readiness for the domain switch): a code audit found structured data (JSON-LD) was completely absent site-wide, and canonical tags existed only on the PDP. Fixed both:
  - **New `app/lib/seo.ts`** — `canonicalLink(pathname)` (relative hrefs on purpose, same as the PDP's pre-existing canonical — resolves against whichever origin actually serves the page, so it needs zero changes at the `amargranth.com` cutover) plus JSON-LD builders: `organizationJsonLd`, `websiteJsonLd`, `productJsonLd`, `articleJsonLd`, `breadcrumbJsonLd`. **New `app/components/JsonLd.tsx`** renders them with the same CSP nonce pattern as `MetaPixel.tsx`/`GoogleAnalytics.tsx` (`script-src` governs every `<script>` tag regardless of type, JSON-LD included).
  - **Organization + WebSite JSON-LD** render site-wide from `root.tsx`'s `<head>`, using the real logo asset and `site-config.ts` data (name, description, contact email, social URLs).
  - **Product JSON-LD + BreadcrumbList JSON-LD** added to the PDP (`products.$handle.tsx`) — real price/currency/availability/SKU/images from the already-fetched product data, no new GraphQL fields needed.
  - **Article/BlogPosting JSON-LD** added to the blog article page (`blogs.$blogHandle.$articleHandle.tsx`) — headline/image/datePublished/author from the existing `ARTICLE_QUERY`.
  - **Canonical tags added to all 13 previously-missing indexable routes**: homepage, both collection listing routes + individual collection pages, both blog listing routes (the per-blog listing route self-references including `?tag=`, since filtered category views are real shareable content, not canonicalized away) + article pages, about/contact/FAQ, both policy routes, and the generic CMS page route. Only non-indexable routes (account/auth, cart actions, redirects, robots/sitemap) still lack one, correctly.
  - **New `app/routes/[llms.txt].tsx`** — same loader pattern as the existing `[robots.txt].tsx`, returns a plain-text brand/catalog/policy summary for AI answer engines. Content mirrors real, already-established copy from `about.tsx`/`faq.tsx`/`site-config.ts` rather than inventing anything; deliberately omits prices since those live in Shopify and can change without a code deploy.
  - Re-ran the Section 0.1 hardcoded-color grep scan after this pass — still clean (JSON-LD/canonical additions touch no colors).
- Verified throughout: `npx tsc --noEmit`, `eslint`, and `npm run build` all pass clean.
- **Deployment + DNS/go-live are DONE.** The Hydrogen app was deployed to Oxygen (`npx shopify hydrogen deploy` → Production), and `amargranth.com`/`www.amargranth.com` are now connected to the **"Amargranth (Production)" Oxygen channel** in Shopify Admin → Settings → Domains — not the classic "Online Store" channel, which is where they were nested by default after DNS first connected (a real gap: a Hydrogen storefront doesn't automatically receive traffic just because a domain is "Connected" in Shopify — the domain has to be explicitly reassigned to the Hydrogen/Oxygen channel, and in this case also set as that channel's Primary domain, or it silently keeps routing to the unconfigured classic Online Store). DNS itself lives with Wix (`ns0/ns1.wixdns.net` nameservers, even though the domain is registered at GoDaddy) — the actual A/CNAME records were edited inside Wix's own domain DNS panel, not GoDaddy's.
- **Real shipping-rate bug found and fixed post-launch:** the very first live checkout test showed a shipping charge despite the site's explicit "Free shipping across India, no minimum" copy (about/FAQ/trust rows, confirmed with ASM earlier in this project). Queried the shop's live `deliveryProfiles` via the Admin API and found the Domestic (India) zone had a real, non-zero rate — a method definition named "मानक" ("Standard") at **₹379** — left over from initial store setup and never zeroed to match the free-shipping promise; this was never something this codebase's copy controlled, since Shopify's real checkout (not the Hydrogen storefront) calculates shipping entirely from Admin's Shipping settings. Fixed via `deliveryProfileUpdate` (`methodDefinitionsToUpdate` → `rateDefinition.price` → `0.00 INR`), verified by re-querying afterward. **Left the International zone's ₹1,800 rate untouched** — the site has never claimed free international shipping, only "across India." If international shipping needs its own free/discounted rate later, that's a deliberate business decision to make explicitly, not a bug to silently fix. Also renamed that same method definition's display name from "मानक" to **"Standard Shipping"** (same `deliveryProfileUpdate` mutation, `name` field) — the Hindi label read oddly against an otherwise all-English checkout, so it now shows "Standard Shipping · 3 to 5 business days · FREE" at checkout.
- **Real mobile bug found post-launch: mobile browser chrome was covering the Checkout button.** A real-device screenshot on iOS showed the Checkout button (in both the cart drawer and the standalone `/cart` page — both render the shared `CartSummary`) sitting flush against the bottom of the page, with Safari's bottom toolbar overlapping/blocking it, making it untappable. Root cause: the codebase had zero `env(safe-area-inset-*)` handling anywhere, **and** the viewport meta tag was missing `viewport-fit=cover` — without that flag, `env(safe-area-inset-bottom)` always evaluates to `0` on iOS regardless of how much padding code adds, so this wasn't fixable by padding alone. Fixed both: `root.tsx`'s viewport meta now includes `viewport-fit=cover`, and `CartSummary.tsx`'s outer container's bottom padding is now `pb-[max(1.5rem,env(safe-area-inset-bottom))]` (keeps the existing 24px on devices without a safe-area inset, grows to clear it on ones that have it — e.g. the iPhone home-indicator area). Also found and fixed the same latent gap on the **PDP's sticky mobile add-to-cart bar** (`products.$handle.tsx`, `fixed bottom-0` — same class of bug, just not yet reported) with the equivalent `pb-[max(0.75rem,env(safe-area-inset-bottom))]` treatment. The sticky `Header` was checked too but doesn't need this — it's `position: sticky`, not `fixed`, so it doesn't have the same safe-area exposure. **This is the kind of bug that's essentially invisible without a real device/browser** — nothing in a desktop check or even browser DevTools' mobile emulation reliably surfaces it, since emulation doesn't model the real toolbar chrome overlapping the visual viewport the way a real phone does.

- **Exit-intent offer banner** (Sept 2026, `app/components/ExitIntentBanner.tsx`, mounted site-wide in `PageLayout.tsx`): shows a real, live Shopify discount code (`EXTRA10`, 10% off the entire order, no end date, all customers — created via the Admin API, `gid://shopify/DiscountCodeNode/1565268803839`) when a visitor looks like they're about to leave. **Desktop** uses the standard `mouseleave`-toward-top-of-viewport signal (`clientY <= 0`, no `relatedTarget`, i.e. genuinely leaving the page rather than moving to another element). **Mobile has no cursor**, so — since ASM's traffic is predominantly Meta (Facebook/Instagram) ads and mobile was explicitly flagged as the priority — it uses the two signals real commercial exit-intent tools (OptinMonster, Privy) actually ship for touch devices: a fast upward scroll near the top of the page after genuine scroll-depth engagement (the "giving up and reaching for the address bar" gesture — velocity + minimum-delta thresholds, gated behind an 8s arm delay so it can't fire instantly), and a 45s idle-timer fallback for visitors who stop interacting without scrolling back up. **Deliberately did NOT implement a `history.pushState`/`popstate` back-button trap**, despite it being a commonly cited mobile exit-intent technique — it requires a second back-press to actually leave, which is a real navigation anti-pattern and a bad first impression specifically for paid ad traffic landing here (this was a judgment call made explicitly in favor of real UX/ad-quality experience over a naive "trap the user" approach when the user asked for "standard best practice" without specifying a mechanism). Shows at most once per day per visitor (`localStorage` timestamp gate) and is suppressed entirely on `/cart` and `/account` routes and while the cart/mobile-menu drawer is open. UI: a bottom-sheet on mobile / centered modal on desktop (`rounded-t-card sm:rounded-card`), safe-area-aware bottom padding matching the pattern already established for the cart summary and sticky PDP bar, `--color-badge-sale` for the urgency eyebrow (a real, time-unbounded discount still reads as an urgency-style offer, not a positive/social-proof badge), `--color-accent` for the single CTA, copy-to-clipboard for the code with a 2s "Copied!" confirmation, close via the × button, the overlay click, or Escape (same three-path pattern as the cart drawer). Ran the Section 0.1 hardcoded-color grep scan — clean (the two `&#8722;` HTML-entity false-positive hits it always surfaces are pre-existing and unrelated).
  - **Trigger condition centralized into one config (ASM follow-up request).** ASM asked to be able to switch what shows the banner — exit intent, abandoned cart, abandoned checkout, or any combination — from a single place, without hunting through the component. **New `app/lib/exitBannerConfig.ts`** now owns that: `exitBannerConfig.triggers = {exitIntent, abandonedCart, abandonedCheckout}` (currently `{true, false, false}`, i.e. unchanged behavior) is the only thing that needs editing to change which condition(s) are live — `ExitIntentBanner.tsx` itself never needs touching again for this. All three modes share the exact same physical "about to leave" detection (desktop mouseleave, mobile scroll-up/idle) described above; they differ only in an extra eligibility check evaluated inside `triggerBanner()`: `abandonedCart` additionally requires the visitor's real cart (via `useOptimisticCart`, the same hook `Header.tsx`'s cart badge uses — `ExitIntentBanner` now takes the root `cart` promise as a prop, resolved through the same `Suspense`/`Await` pattern) to have `totalQuantity > 0`; `abandonedCheckout` additionally requires that AND a recent (within 30 min) `markCheckoutStarted()` call. That helper is called at the two real places a visitor leaves for Shopify's hosted checkout — `CartSummary.tsx`'s Checkout link and `ProductForm.tsx`'s Buy Now button (the `/cart/$lines` direct-link redirect route wasn't instrumented — it's for external marketing links, not something visitors click from within the site's own UI) — via `sessionStorage`, so a later page load on the storefront can tell "came back without completing" apart from an ordinary first visit; Shopify's own checkout domain is architecturally separate, so this is the only reliable client-side signal available without a webhook. Any combination of the three booleans can be `true` at once — whichever enabled condition is met first shows the banner, still capped at once/day/visitor as before. Verified pipeline (`tsc`, `eslint`, `npm run build`, Section 0.1 grep) clean.

- **Real mobile bug found from a screenshot: PDP promo banner turned into a distorted stadium shape when its text wrapped.** A real iPhone 14 screenshot of the "Ganpati Festive Sale" event banner (`products.$handle.tsx`, the metafield-driven promo banner described above) showed its text wrapping to 2 lines while the container still had `rounded-pill` (999px radius) — on a 2-line box that produces a stretched oval/blob rather than a banner, since a stadium radius only looks right on a single-line pill. Fixed by switching the container to `rounded-card` (the doc's standard 1rem radius, already used for cards/callouts elsewhere), so it reads correctly whether a given promo's label/discount/date text fits one line or wraps to two — this is future-proof against future metafield edits in Admin, not just today's copy.

### Explicitly NOT done yet
1. **Razorpay** — in progress, see note above. Needs ASM to finish adding all relevant Razorpay payment-method listings in Admin and confirm they're active.
2. **Facebook & Instagram sales channel** and **Google & YouTube sales channel** — needs ASM to connect both in Shopify Admin (their Meta Business account and Google account respectively) to get Purchase-event tracking for both pixels (see notes above). The Google side has at least a Merchant Center ID now (`778061429`, see above) — whether the "Google & YouTube" channel itself is fully connected/syncing in Admin, and whether Facebook & Instagram is connected at all, hasn't been confirmed in this conversation.
3. **Meta Conversions API (CAPI)** — optional, more reliable server-side tracking. Not set up; would need a CAPI access token from Meta Events Manager if ASM wants it.
4. **Real browser/device verification** — the site is now genuinely live at `amargranth.com` (see go-live note above), so this is finally checkable directly rather than only via Codespaces preview. This sandbox's network egress still blocks every Shopify host (confirmed again during the go-live process — even the raw Oxygen deployment URL and a plain DNS lookup were blocked), so Claude Code still cannot visually verify anything here; ASM has to check directly. **Every `DESIGN_SYSTEM.md` page rebuilt in this codebase (homepage, PDP, PLP, cart, blog listing/article) is still formally unverified against Section 12's own three-widths checklist (375px/768px/1440px)** — spot-checks have happened along the way (mobile fixes, logo, the shipping bug caught on a real checkout test), but the full sweep hasn't been done as a single pass.
5. **Customer-specific personalization** — the PDP's "you already own this" ownership badge from `DESIGN_SYSTEM.md` 7.3 needs Customer Account API / order-history integration that doesn't exist in this codebase. The *product-to-product* half of the ownership-equivalence rule (never recommend standalone Jyotirlingas/Shaktipeeths alongside the Combo Set) IS implemented, tag-driven, on the PDP's related-products rail — just not the per-customer badge.
6. **`DESIGN_SYSTEM.md`'s finer-grained checklist items** — a second audit pass (against a re-uploaded, expanded version of the doc adding Sections 2.1, 4's container recipe, 5's button-state table, and 5.1 card boundaries) fixed: card boundaries site-wide (every product/blog/collection card now has a white background + border + shadow via a shared `.card` class — previously cards were bare pastel tiles with no visible edge), page-container padding consistency (Header/Footer/about/contact/faq/homepage now all use the doc's exact 20px/48px/64px breakpoint padding, not an older 24px/64px pattern that skipped the tablet step), hero spacing precision (2.1's exact values), global `:focus-visible` states, and `active:` button states. It also computed real contrast ratios and darkened the accent color (see Colors above) since the original failed AA. **Still not done:** the full Section 2 spacing-scale trace file-by-file, a complete Section 5 contrast pass beyond the accent color (e.g. verifying every tint-background + text pairing individually), and a customer-reviews section (blocked on ASM supplying real quotes — see above).
7. ~~**DNS / go-live**~~ — **DONE.** See the go-live note in the "Done" section above.

### Notes / gotchas for whoever picks this up
- Do **not** touch DNS or suggest connecting the `amargranth.com` domain to this Shopify store until ASM has explicitly signed off in this exact conversation — this was asked about directly and deliberately held back pending readiness (products were DRAFT and FAQ was placeholder at the time; both are now fixed, but the explicit go-ahead and a real browser check are still outstanding).
- Product descriptions contain time-sensitive promo copy migrated from Wix (e.g. "Ganpati Festive Sale... Valid till 30th September 2026") — worth knowing it's baked into the description HTML, not something this codebase added.
- Full redirect map (66 blog + 7 product) is already live in Shopify's URL redirect system — nothing needed there, it'll just work once the domain is repointed.
- **`app/styles/reset.css` must stay wrapped in `@layer base { ... }`.** It's a leftover Hydrogen-skeleton stylesheet loaded via its own `<link>` tag. Tailwind v4 puts its generated utility classes in `@layer utilities`; if reset.css's rules are ever unwrapped back to plain unlayered CSS, its bare `a { color: #000 }` / `p { font-size: 1rem }` / `body { font-family: system-ui... }` will silently beat `text-white`/`text-body-lg`/the Inter font-family on any element that doesn't carry an extra overriding class — regardless of specificity, since unlayered CSS always wins over layered CSS. This is exactly what caused a black "Shop the collection" button (white text was being silently overridden) — found from the first real-browser screenshot this session, after a lot of styling work had already shipped un-verified. If a future screenshot shows an unexpected default browser color/size on text, check this file's layer wrapping first.
